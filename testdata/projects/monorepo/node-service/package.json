{
  "name": "node-service",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage --coverage.reporter=lcov"
  },
  "devDependencies": {
    "vitest": "^3",
    "@vitest/coverage-v8": "^3"
  }
}
